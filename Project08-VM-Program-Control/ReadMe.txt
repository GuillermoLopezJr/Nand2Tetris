To compile files type into the command prompt:

javac Main.java

To run type (without quotation marks):


java Main "file_name.vm"
		or
java Main "directory_name"

add the flag -n if init code is not desired.
